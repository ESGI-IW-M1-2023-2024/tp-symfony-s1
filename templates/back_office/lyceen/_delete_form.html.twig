<form method="post" action="{{ path('app_lyceen_delete', {'id': lyceen.id}) }}" onsubmit="return confirm('Etes-vous sûr de bien vouloir supprimer ce lycéen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lyceen.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
