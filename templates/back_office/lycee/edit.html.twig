{% extends 'base.html.twig' %}

{% block title %}Edit Lycee{% endblock %}

{% block body %}
    <h1 class="text-primary text-center">Modification du lycée</h1>
    {{ include('back_office/lycee/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="d-flex gap-2 mt-2">
        <a class="btn-primary btn " href="{{ path('app_lycee_index') }}">Retour à la liste</a>
        {{ include('back_office/lycee/_delete_form.html.twig') }}
    </div>
{% endblock %}
