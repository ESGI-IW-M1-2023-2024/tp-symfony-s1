{% extends 'base.html.twig' %}

{% block title %}Lycee{% endblock %}

{% block body %}
    <h1 class="text-center text-primary">Lycee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lycee.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lycee.nom }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex gap-2">
        <a class="btn btn-primary" href="{{ path('app_lycee_index') }}">Retour à la liste</a>
        <a class="btn btn-primary" href="{{ path('app_lycee_edit', {'id': lycee.id}) }}">Modifier</a>
        {{ include('back_office/lycee/_delete_form.html.twig') }}
    </div>

{% endblock %}
