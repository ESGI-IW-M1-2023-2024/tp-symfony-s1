<form method="post" action="{{ path('app_atelier_delete', {'id': atelier.id}) }}" onsubmit="return confirm('Etes-vous sûr de bien vouloir supprimer cet atelier?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ atelier.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
