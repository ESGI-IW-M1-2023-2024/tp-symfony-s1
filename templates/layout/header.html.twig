<div class="container">
  <header
    class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">


    <div class="col-md-3 text-center d-flex">
        <a href="{{ path('app_home') }}" class="nav-link px-2 link-secondary">Accueil</a>
        <a href="{{ path('app_home') }}" class="nav-link px-2 link-secondary">Les ateliers</a>
    </div>

    <div class="col-md-3 mb-2 mb-md-0">
      <a href="{{ path('app_home') }}" class="d-inline-flex link-body-emphasis text-decoration-none">
        <svg class="bi" width="40" height="32" role="img" aria-label="Bootstrap">
          <use xlink:href="#bootstrap"></use>
        </svg>
      </a>
    </div>
        {% if app.user %}
            <p class="m-auto text-secondary px-2">{{ app.user.userIdentifier }}</p>
            <a type="button" class="btn btn-primary" href="{{ path('app_logout') }}">Déconnexion</a>
        {% else %}
            <div class="d-flex">
                <a type="button" class="btn btn-outline-primary me-2" href="{{ path('app_login') }}">Se connecter</a>
                <a class="btn btn-primary" href="{{ path("app_register") }}">S'inscrire</a>
            </div>
        {% endif %}
  </header>
</div>
